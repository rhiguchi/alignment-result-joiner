framework: mocha+chai

launch_in_dev:
- PhantomJS

on_start: mkdir -p .testem
on_exit: rm -r .testem

before_tests: coffee -o .testem -c src/client.coffee test/client/*.coffee

src_files:
- src/client.coffee
- test/client/*.coffee

serve_files:
- .testem/*.js
